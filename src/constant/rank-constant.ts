const RANK_TYPE: {
    RANK_TODAY: number,
    RANK_WEEK: number,
    RANK_MONTH: number,
} = (function () {
    return {
        RANK_TODAY: 1,
        RANK_WEEK: 2,
        RANK_MONTH: 3,
    }
}())

export {
    RANK_TYPE,
}